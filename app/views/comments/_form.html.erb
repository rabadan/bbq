<%= simple_form_for [@event, comment] do |f| %>
    <%= render partial: 'partials/forms_errors', locals: {model: comment} %>

    <%= f.input :body %>

    <% unless user_signed_in? %>
        <%= f.input :user_name %>
    <% end %>

    <%= f.button :submit, class: 'btn btn-primary'  %>
<% end %>